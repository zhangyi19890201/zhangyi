# 人力资源管理系统开发文档

## 1. 系统概述

人力资源管理系统是一个完整的员工信息管理解决方案，旨在帮助企业高效管理员工数据、证书信息和劳务派遣人员信息。系统提供了全面的员工信息管理、证书跟踪、数据导入导出等功能，支持在职与非在职员工的无缝流转。

## 2. 系统架构

### 2.1 整体结构

系统采用React单页应用架构，主要由一个主页面和五个子系统模块组成。各模块之间通过路由进行导航，数据在模块内部通过localStorage进行临时存储。

```
├── src/
│   ├── pages/
│   │   └── hr/
│   │       ├── HRMainPage.tsx       # 人力资源管理系统主页
│   │       ├── CurrentEmployeeSystem.tsx      # 在职员工信息系统
│   │       ├── FormerEmployeeSystem.tsx       # 非在职员工信息系统
│   │       ├── EmployeeDocumentSystem.tsx     # 员工证书信息管理系统
│   │       ├── ContractEmployeeSystem.tsx     # 劳务派遣信息系统
│   │       └── FormerContractEmployeeSystem.tsx  # 非在职劳务派遣信息系统
│   └── utils/
│       ├── fileUtils.ts      # 文件处理工具
│       └── idUtils.ts        # ID和日期处理工具
├── package.json              # 项目依赖配置
```

### 2.2 模块关系

系统各模块之间的关系如下：

- 在职员工系统与非在职员工系统：删除在职员工时，自动将员工信息转移到非在职员工系统
- 劳务派遣系统与非在职劳务派遣系统：删除劳务派遣人员时，自动将信息转移到非在职劳务派遣系统
- 员工证书系统与在职员工系统：证书系统关联在职员工，当在职员工被删除时，其证书信息也会被删除

## 3. 核心功能模块

### 3.1 在职员工信息系统 (CurrentEmployeeSystem)

**功能描述**：管理公司在职员工的基本信息，支持员工信息的新增、修改、删除和查询。

**主要功能点**：
- 员工信息的增删改查
- Excel数据导入导出
- 员工搜索（按姓名、工号、身份证号）
- 分页显示
- 删除员工时自动转移到非在职系统
- 生成测试数据功能

**核心代码结构**：
```tsx
// 员工信息接口定义
interface Employee {
  id: string;
  employeeId: string;
  name: string;
  idNumber: string;
  gender: 'male' | 'female';
  age: number;
  department: string;
  position: string;
  education: string;
  hireDate: string;
  phone: string;
  email: string;
}

// 主要状态管理
const [employees, setEmployees] = useState<Employee[]>([]);
const [filteredEmployees, setFilteredEmployees] = useState<Employee[]>([]);
const [currentPage, setCurrentPage] = useState(1);
const [pageSize, setPageSize] = useState(10);

// 核心功能函数
const handleSaveEmployee = () => { /* 添加/编辑员工逻辑 */ };
const handleDeleteEmployee = (id: string) => { /* 删除员工并转移到非在职系统 */ };
const exportDataToExcel = () => { /* 导出数据到Excel */ };
const importDataFromExcel = (file: File) => { /* 从Excel导入数据 */ };
```

### 3.2 非在职员工信息系统 (FormerEmployeeSystem)

**功能描述**：管理已经离职的员工信息，支持查询和导出功能。

**主要功能点**：
- 查看离职员工信息
- Excel数据导入导出
- 员工搜索
- 分页显示

**核心代码结构**：
```tsx
// 非在职员工信息接口定义
interface FormerEmployee {
  id: string;
  employeeId: string;
  name: string;
  idNumber: string;
  gender: 'male' | 'female';
  age: number;
  department: string;
  position: string;
  education: string;
  hireDate: string;
  leaveDate: string;
  leaveReason: string;
  archiveDate: string;
  phone: string;
}

// 主要状态管理
const [employees, setEmployees] = useState<FormerEmployee[]>([]);
const [filteredEmployees, setFilteredEmployees] = useState<FormerEmployee[]>([]);
```

### 3.3 员工证书信息管理系统 (EmployeeDocumentSystem)

**功能描述**：管理在职员工的各类证书信息，包括证书名称、编号、有效期等。

**主要功能点**：
- 证书信息的增删改查
- 证书有效期提醒
- 证书状态自动计算
- 员工搜索与证书搜索
- 分页显示

**核心代码结构**：
```tsx
// 证书信息接口定义
interface Certificate {
  id: string;
  employeeId: string;
  employeeName: string;
  certificateName: string;
  certificateNumber: string;
  issueDate: string;
  expiryDate: string;
  status: 'valid' | 'expiring' | 'expired';
  department: string;
}

// 证书状态计算
const calculateStatus = (expiryDate: string): 'valid' | 'expiring' | 'expired' => {
  /* 根据过期日期计算证书状态 */
};
```

### 3.4 劳务派遣信息系统 (ContractEmployeeSystem)

**功能描述**：管理公司的劳务派遣人员信息，支持信息的新增、修改、删除和查询。

**主要功能点**：
- 劳务派遣人员信息的增删改查
- Excel数据导入导出
- 人员搜索
- 分页显示
- 删除人员时自动转移到非在职劳务派遣系统
- 根据身份证号自动计算性别和年龄

**核心代码结构**：
```tsx
// 劳务派遣人员信息接口定义
interface ContractEmployee {
  id: string;
  employeeId: string;
  name: string;
  idNumber: string;
  gender: 'male' | 'female';
  age: number;
  department: string;
  position: string;
  project: string;
  employmentAgency: string;
  contractStartDate: string;
  contractEndDate?: string;
  salary: number;
  paymentMethod: string;
  status: 'active' | 'expired' | 'renewed';
}

// 删除员工并转移到非在职系统
const handleDeleteEmployee = (employeeId: string) => { /* 删除逻辑 */ };
```

### 3.5 非在职劳务派遣信息系统 (FormerContractEmployeeSystem)

**功能描述**：管理已经结束派遣的人员信息，支持查询和导出功能。

**主要功能点**：
- 查看已结束派遣的人员信息
- Excel数据导入导出
- 人员搜索
- 分页显示
- 离职手续状态管理
- 派遣时长计算

**核心代码结构**：
```tsx
// 非在职劳务派遣人员信息接口定义
interface FormerContractEmployee {
  id: string;
  employeeId: string;
  name: string;
  idNumber: string;
  gender: 'male' | 'female';
  age: number;
  department: string;
  position: string;
  project: string;
  employmentAgency: string;
  contractStartDate: string;
  contractEndDate?: string;
  leaveDate: string;
  reasonForLeaving: string;
  employmentDuration: string;
  clearanceStatus: 'completed' | 'pending' | 'incomplete';
}

// 计算派遣时长
const calculateEmploymentDuration = (startDate: string, endDate: string): string => { /* 计算逻辑 */ };

// 获取离职手续状态信息
const getClearanceStatusInfo = (status: string) => { /* 状态转换逻辑 */ };
```

## 4. 工具函数库

### 4.1 文件处理工具 (fileUtils.ts)

**功能描述**：提供Excel文件的导入导出功能和localStorage数据操作功能。

**主要函数**：
- `exportToExcel(data: any[], filename: string, sheetName?: string)`：将数据导出为Excel文件
- `importFromExcel(file: File): Promise<any[]>`：从Excel文件导入数据
- `isExcelFile(file: File): boolean`：验证文件是否为Excel文件
- `getFromLocalStorage(key: string, defaultValue: any = null): any`：从localStorage获取数据
- `saveToLocalStorage(key: string, value: any): void`：保存数据到localStorage

### 4.2 ID和日期处理工具 (idUtils.ts)

**功能描述**：提供身份证号解析、日期格式化和唯一ID生成等功能。

**主要函数**：
- `getGenderFromIdNumber(idNumber: string): 'male' | 'female'`：根据身份证号获取性别
- `getBirthDateFromIdNumber(idNumber: string): string`：根据身份证号获取出生日期
- `calculateAgeFromIdNumber(idNumber: string): number`：根据身份证号计算年龄
- `calculateEmploymentDuration(startDate: string, endDate: string): string`：计算派遣时长
- `validateIdNumber(idNumber: string): boolean`：验证身份证号格式
- `generateUniqueId(): string`：生成唯一ID
- `formatDate(date: Date | string): string`：格式化日期

## 5. 技术实现细节

### 5.1 前端技术栈

- **React 19.1.1**：用于构建用户界面的JavaScript库
- **TypeScript 5.9.2**：为JavaScript添加类型支持
- **React Router 7.9.2**：用于实现路由功能
- **XLSX 0.18.5**：用于Excel文件的导入导出
- **Vite 7.1.7**：用于项目构建和开发服务器

### 5.2 数据存储

系统使用localStorage进行数据的临时存储，主要存储以下数据：
- currentEmployees：在职员工数据
- formerEmployees：非在职员工数据
- contractEmployees：劳务派遣人员数据
- formerContractEmployees：非在职劳务派遣人员数据
- employeeCertificates：员工证书数据

### 5.3 用户界面设计

系统采用简洁明了的UI设计，主要特点包括：
- 左侧导航栏：用于在不同子系统之间切换
- 顶部标题栏：显示当前模块名称
- 搜索栏：用于快速查找员工或证书
- 功能按钮区：包含新增、导入、导出等操作按钮
- 数据表格：显示员工或证书列表
- 分页组件：用于分页浏览数据
- 模态框：用于新增、编辑和查看详情

### 5.4 核心业务流程

#### 5.4.1 员工删除流程

1. 用户在在职员工系统中选择要删除的员工
2. 系统弹出确认对话框，提示删除后将转移到非在职系统
3. 用户确认删除
4. 系统创建非在职员工记录，包含原员工信息和离职相关信息
5. 系统将新创建的非在职员工记录添加到非在职员工数据中
6. 系统从在职员工列表中移除该员工
7. 系统更新localStorage中的数据

#### 5.4.2 数据导入流程

1. 用户点击导入按钮，选择Excel文件
2. 系统验证文件类型是否为Excel
3. 系统读取Excel文件内容并解析为JSON数据
4. 系统根据Excel表头映射数据字段
5. 系统将解析后的数据添加到现有数据中
6. 系统更新localStorage中的数据
7. 系统提示导入成功信息

#### 5.4.3 数据导出流程

1. 用户点击导出按钮
2. 系统准备要导出的数据，选择需要的字段
3. 系统创建Excel工作簿和工作表
4. 系统将数据写入工作表
5. 系统触发文件下载，用户选择保存路径

## 6. 项目配置与部署

### 6.1 依赖安装

```bash
pnpm install
```

### 6.2 开发模式运行

```bash
pnpm run dev
```

### 6.3 项目构建

```bash
pnpm run build
```

构建后的文件将生成在`dist`目录中，可以部署到任何静态文件服务器上。

## 7. 注意事项与限制

1. **数据存储限制**：系统使用localStorage存储数据，存在存储空间限制（通常为5-10MB）
2. **数据持久性**：localStorage中的数据在用户清除浏览器缓存或使用隐私模式时可能丢失
3. **浏览器兼容性**：建议使用现代浏览器（Chrome、Firefox、Safari、Edge）以获得最佳体验
4. **性能优化**：对于大量数据（如150+条记录），系统提供了分页功能以优化性能
5. **身份证号验证**：系统提供了基本的身份证号格式验证，但不保证绝对准确
6. **安全性**：系统未实现用户认证和权限控制，仅作为演示用途

## 8. 未来优化方向

1. 集成后端API，替换localStorage存储
2. 实现用户认证和权限控制
3. 增加数据可视化报表功能
4. 优化大量数据下的性能
5. 增加更多的员工信息字段和统计功能
6. 实现移动端适配