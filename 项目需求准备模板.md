# 项目需求准备模板

本模板帮助您在AI开始编程前，整理和准备所有必要的项目信息。请尽可能详细地填写以下内容，这将有助于AI更准确地理解您的需求并提供高质量的代码实现。

## 一、需求文档/功能说明

### 1. 项目概述
请简要描述您想要实现的项目或解决的问题：

开发一个公司级别的工程项目管理软件，要涵盖公司的事业部、采购部、财务中心、人力资源部、经营管理中心、法务部、研发技术中心、总经办、行政管理部、品牌发展部、证券投资部、审计监察部，各部门分别独立开发功能模块，所有变量都在同一文件内，各模块开发时，其他模块通过引用变量的方式使用，禁止功能模块随便定义变量。

### 2. 核心功能点
请列出项目的核心功能点（可按优先级排序）：

| 优先级 | 功能点描述 | 详细说明 |
|--------|------------|----------|
| 1 | 变量模块开发 | 所有变量都在同一文件内，各模块开发时，其他模块通过引用变量的方式使用，禁止功能模块随便定义变量 |
| 2 | 用户注册/登录 | 以项目PM管理系统为主题的登录页面，测试用户名：a、测试密码a，具有记住用户名、记住密码的功能，登录后进入管理系统工作页面，正式用户名为用户员工号，密码默认aaaaaa，首次登录后，支持修改用户名和密码，但修改后的用户名在员工信息里面与员工号关联，修改用户名后，使用员工号和新密码登录一样登录 |
| 3 | 流程设计 | 可视化页面内进行各流程设计及保存，并可将设计好的流程嵌入对应的工作模块的对应工作；对存在的流程进行可视化页面修改及保存，保存后，后续发起的审批，按修改后的流程进行审批 |
| 4 | 人力资源信息管理系统 | 该系统分成5个子系统：在职员工信息系统、非在职员工信息系统、员工证书信息登记系统、劳务派遣信息系统、非在职劳务派遣信息系统；支持公司员工信息新增、修改、删除，对删除后的员工信息自动转入非在职员工信息系统；员工证书信息登记系统对在职员工的各类证书进行登记，删除在职员工共信息后，证书信息删除，不在保存；支持劳务派遣信息新增、修改、删除，对删除后的劳务派遣信息自动转入非在职劳务派遣信息系统 |
| 5 | 采购管理系统 | 采购管理系统包括采购设备及材料的招标管理、采购合同管理、采购发票管理、采购付款管理等功能，支持采购订单的新增、修改、终止；专业分包和劳务分包的招标管理，专业分包和劳务分包的合同由事业部进行管理，不在采购管理系统内 |
| 6 | 财务中心管理系统 | 财务中心管理系统包括采员工报销、员工借款、专业分包工程款支付及稽核、劳务分包工程款支付及稽核、设备合同付款及稽核、材料合同付款及稽核、其他合同付款及稽核、发票冲账、差旅费用报销、携程差旅费用报销、开票申请、银企直连； |
| ... | ... | ... |

### 3. 用户使用流程
请描述典型的用户使用场景和流程：
用户登录后，首先显示待审批任务列表、发起任务列表、已办任务列表，首页只对每个列表最多显示5条记录，用户可以点击审批任务列表右侧的"》"按钮；进入查看所有审批任务记录，点击具体任务进行审批，审批包括：意见框（默认内容为：通过，请填写意见）、通过、不通过（点击不通过，必须在意见框内填写不通过原因）、退回（点击退回，必须在意见框填写退回原因）、转交（转交后，不在参与本次任务审批）、审批加签（点击审批加签，需要选择加签人员，本人可以选择在加签前审批或加签后审批，加签人员可以转交、通过、不通过、退回、加签；退回或不通过，流程即终止，不在继续审批；流程发起人可以对退回或不通过的流程重新编辑，再次发起审批，按设定的审批流程审批）；用户可以点击发起任务列表右侧的"》"按钮，进入查看所有发起任务记录，点击具体任务，查看详细任务内容，并可查看审批情况；用户可以点击已办任务列表右侧的"》"按钮，进入查看所有已办任务记录，点击具体任务，查看详细任务内容。
> 示例：用户登录后，在首页查看今日待办事项列表，点击"添加"按钮创建新任务，设置任务详情后保存，任务会显示在列表中。用户可以点击任务标记为完成或编辑任务详情。

### 4. 参考示例
暂无

## 二、技术栈要求

### 1. 编程语言
请指定您希望使用的编程语言：Python 3.8+

> 示例：JavaScript、Python、Java等

### 2. 框架偏好
请指定您希望使用的框架：Django

> 示例：前端（React、Vue、Angular）、后端（Express、Django、Spring Boot）等

### 3. 数据库要求
请指定您希望使用的数据库：MySQL 8.0+

> 示例：MySQL、MongoDB、PostgreSQL等

### 4. 特殊技术或库
请列出您希望使用的特殊技术或第三方库：‌React Hooks

> 示例：React Hooks、Redux、Axios、Tailwind CSS等

## 三、界面设计要求（如涉及UI）

### 1. 设计稿或参考图
如果有设计稿或参考图，请提供链接或描述：无

> 示例：[Figma设计稿链接](https://figma.com/example)

### 2. 颜色方案
请提供品牌主色调和辅助色：浅蓝色、黑色、白色，可以根据需要添加其他颜色。

> 示例：主色 #165DFF，辅助色 #0FC6C2，中性色 #F5F7FA

### 3. 交互细节
请描述关键交互细节和动画效果要求：
1. 点击审批任务列表中的记录，弹出审批任务详情弹窗，用户可以查看详情、编辑、审批任务等操作。
2. 当任务发起后，同时在发起任务列表中新增一条记录。
3. 当审批任务后，同时在已办任务列表中新增一条记录。
4. 点击已办任务列表或发起任务列表中的记录，弹出已办任务或已发起任务的详情弹窗，用户只可查看，不可修改。
5. 任务发起人可以对退后或不通过任务进行修改，正常在途任务，只有当前审批人可以修改，并保存修改记录，其他审批人和发起人无法修改。

> 示例：添加待办事项时的淡入动画，完成任务时的勾选动画，下拉刷新功能

### 4. 响应式设计
请描述对不同设备的适配要求：
1. 支持移动端（360px+）、平板（768px+）、桌面端（1200px+）等进行自动适配。
2. 桌面端可发起任务，移动端及平板端只能查看任务和审批任务，不能发起任务；当流程到达审批节点时，审批节点的审批人所登录的设备应给出通知进行提醒。
3. 当任务发起后，发起人可以在发起任务列表中查看任务详情，也可以在具体任务模块的任务列表中查看。

> 示例：支持移动端（360px+）、平板（768px+）、桌面端（1200px+）适配

## 四、数据结构和API需求

### 2. API接口需求
请定义主要的API接口：

| API路径 | 方法 | 功能描述 | 参数 | 响应 |
|---------|------|----------|------|------|
| /api/auth/login | POST | 用户登录 | {username, password, remember_me} | {code, token, user_info} |
| /api/auth/register | POST | 用户注册 | {username, password, email, department_id} | {code, message} |
| /api/auth/change_password | PUT | 修改密码 | {old_password, new_password} | {code, message} |
| /api/tasks | GET | 获取任务列表 | {status, page, page_size} | {code, data: {list, total}} |
| /api/tasks/{id} | GET | 获取任务详情 | {id} | {code, data} |
| /api/tasks | POST | 创建新任务 | {title, content, type, flow_id} | {code, data} |
| /api/tasks/{id}/approve | POST | 审批任务 | {id, comment, action} | {code, message} |
| /api/flows | GET | 获取流程列表 | {page, page_size} | {code, data: {list, total}} |
| /api/flows | POST | 创建流程 | {name, nodes, edges} | {code, data} |
| /api/flows/{id} | PUT | 更新流程 | {id, name, nodes, edges} | {code, message} |
| /api/hr/employees | GET | 获取员工列表 | {status, department_id, page, page_size} | {code, data: {list, total}} |
| /api/hr/employees | POST | 创建员工 | {name, id_card, position, department_id, ...} | {code, data} |
| /api/hr/employees/{id} | PUT | 更新员工 | {id, ...} | {code, message} |
| /api/hr/employees/{id} | DELETE | 删除员工 | {id} | {code, message} |
| /api/purchase/contracts | GET | 获取采购合同列表 | {status, project_id, page, page_size} | {code, data: {list, total}} |
| /api/purchase/contracts | POST | 创建采购合同 | {title, supplier_id, amount, ...} | {code, data} |
| /api/finance/payments | GET | 获取付款列表 | {status, type, page, page_size} | {code, data: {list, total}} |
| /api/finance/payments | POST | 创建付款申请 | {contract_id, amount, reason, ...} | {code, data} |
## 五、项目结构和部署要求

### 1. 项目目录结构
项目采用前后端分离架构，目录结构如下：
```
project_root/
├── backend/             # Django后端项目
│   ├── config/          # 项目配置
│   ├── apps/            # Django应用
│   │   ├── auth/        # 认证相关
│   │   ├── task/        # 任务管理
│   │   ├── flow/        # 流程管理
│   │   ├── hr/          # 人力资源
│   │   ├── purchase/    # 采购管理
│   │   ├── finance/     # 财务管理
│   │   └── ...          # 其他模块
│   ├── utils/           # 工具函数
│   ├── middlewares/     # 中间件
│   ├── migrations/      # 数据库迁移
│   ├── templates/       # 模板文件
│   ├── static/          # 静态文件
│   └── manage.py        # Django管理脚本
├── frontend/            # React前端项目
│   ├── public/          # 静态资源
│   ├── src/             # 源代码
│   │   ├── assets/      # 资源文件
│   │   ├── components/  # 通用组件
│   │   ├── pages/       # 页面组件
│   │   ├── services/    # API服务
│   │   ├── hooks/       # 自定义Hooks
│   │   ├── store/       # 状态管理
│   │   ├── utils/       # 工具函数
│   │   ├── routes/      # 路由配置
│   │   ├── App.jsx      # 应用入口
│   │   └── index.jsx    # 页面入口
│   ├── package.json     # 前端依赖
│   └── vite.config.js   # Vite配置
├── nginx/               # Nginx配置
├── docker-compose.yml   # Docker配置
└── README.md            # 项目说明
```

### 2. 部署环境
请指定您希望的部署环境：
- 操作系统：window11
- 容器化：Docker + Docker Compose
- Web服务器：Nginx
- 应用服务器：Gunicorn (Django) + Node.js (React)
- 数据库：MySQL 8.0+
- 缓存：Redis
- 消息队列：RabbitMQ (用于异步任务)

### 3. 构建和发布流程
请描述您期望的构建和发布流程：
1. 开发环境：
   - 前端：Vite开发服务器 (npm run dev)
   - 后端：Django开发服务器 (python manage.py runserver)
2. 测试环境：
   - CI/CD工具：Jenkins/GitLab CI
   - 自动化测试：单元测试、集成测试
3. 生产环境：
   - 前端构建：npm run build (生成静态文件)
   - 后端部署：Docker容器化部署
   - 静态文件：Nginx直接提供
   - API请求：Nginx反向代理到Gunicorn
   - 数据库迁移：python manage.py migrate
   - 定时任务：Celery + RabbitMQ
4. 发布频率：每周小版本迭代，每月大版本更新


## 六、其他特殊要求

### 1. 性能指标
请描述您对项目的性能要求：

1. API响应时间：90%的请求响应时间小于1秒
2. 页面加载时间：首页加载时间小于2秒，其他页面加载时间小于1.5秒
3. 数据库查询性能：单表查询响应时间小于500ms，复杂查询响应时间小于2秒
4. 并发处理能力：支持1000+用户同时在线操作
5. 系统可用性：99.9%的系统可用性

> 示例：页面加载时间小于2秒，首屏内容渲染时间小于1秒，API响应时间小于500ms

### 2. 安全性要求
请描述您对项目的安全要求：

1. 数据安全：
   - 用户密码加密存储（使用bcrypt等算法）
   - 敏感数据传输加密（使用HTTPS）
   - 定期数据备份和恢复机制
   - 数据访问权限控制

2. 应用安全：
   - API请求鉴权（使用JWT或Session认证）
   - 防止SQL注入、XSS攻击、CSRF攻击
   - 输入数据验证和过滤
   - 错误信息不泄露敏感数据

3. 系统安全：
   - 定期系统漏洞扫描
   - 服务器安全配置（关闭不必要端口、限制IP访问等）
   - 操作日志记录和审计

> 示例：用户密码加密存储，API请求鉴权，防止SQL注入和XSS攻击

### 3. 合规性要求
请描述项目需要遵守的合规性要求：

1. 遵循国家相关法律法规，如《网络安全法》、《数据安全法》等
2. 符合企业内部数据管理规范和安全策略
3. 支持用户数据隐私保护，不滥用用户数据
4. 系统操作可追溯，支持审计和合规检查

> 示例：符合GDPR数据保护法规，支持多语言国际化

### 4. 时间线和里程碑

| 阶段 | 里程碑 | 预计完成时间 | 交付内容 |
|------|--------|--------------|----------|
| 1 | 需求分析和规划 | 第1周 | 需求文档、技术方案、项目计划 |
| 2 | 系统架构设计 | 第2周 | 数据库设计、API设计、系统架构图 |
| 3 | 核心框架开发 | 第3-4周 | 前后端框架搭建、认证授权模块、基础组件库 |
| 4 | 功能模块开发 | 第5-12周 | 人力资源、采购、财务等各模块功能实现 |
| 5 | 系统集成测试 | 第13周 | 集成测试报告、Bug修复 |
| 6 | 用户测试 | 第14周 | 用户测试反馈、功能优化 |
| 7 | 系统上线 | 第15周 | 正式环境部署、用户培训 |
| 8 | 运维和迭代 | 持续 | 系统维护、功能迭代、性能优化

---

请根据您的实际项目需求填写以上内容，越详细的信息将有助于AI提供越精准的代码实现。

